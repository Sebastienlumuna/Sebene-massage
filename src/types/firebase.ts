/* 
 * This file contains types for Firebase Firestore integration.
 * Uncomment and use these types when implementing Firebase.
 */

/*
export interface Booking {
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
  message?: string;
  service: string;
  createdAt: Date;
  status: 'pending' | 'confirmed' | 'cancelled';
}

export interface FirestoreTimestamp {
  toDate(): Date;
  seconds: number;
  nanoseconds: number;
}

export interface FirestoreCollections {
  bookings: Booking[];
}
*/